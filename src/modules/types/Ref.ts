export enum Ref {
	CurrentGameObject = '.',
	CurrentComponent = '#',
	Render = '@render:',

	CoreProxy = '/root#proxy_core',
	MenuProxy = '/root#proxy_menu',

	SafeZonesFactory = '/safe_zones#factory',
}
