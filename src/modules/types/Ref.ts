export enum Ref {
	CurrentGameObject = '.',
	CurrentComponent = '#',

	// main collection
	Main = 'main:/go#main',
	CoreProxy = 'main:/state_loader#core_proxy',
	MenuProxy = 'main:/state_loader#menu_proxy',
}
