local ____exports = {}
local ____engine = require("modules.engine.index")
local DEG_TO_RAD = ____engine.DEG_TO_RAD
local Property = ____engine.Property
local Timeline = ____engine.Timeline
local ____BulletLayout = require("modules.layouts.BulletLayout")
local bulletSchema = ____BulletLayout.bulletSchema
local ____ShooterLayout = require("modules.layouts.ShooterLayout")
local shooterSchema = ____ShooterLayout.shooterSchema
local ____SyncSafeZoneCollider = require("modules.core.helpers.SyncSafeZoneCollider")
local syncSafeZoneCollider = ____SyncSafeZoneCollider.syncSafeZoneCollider
local syncSafeZoneTweenCollider = ____SyncSafeZoneCollider.syncSafeZoneTweenCollider
local ____PhysicsGroup = require("modules.enums.PhysicsGroup")
local PhysicsGroup = ____PhysicsGroup.PhysicsGroup
____exports.Level = __TS__Class()
local Level = ____exports.Level
Level.name = "Level"
function Level.prototype.____constructor(self, ____bindingPattern0)
    local levelData
    local safeZones
    local layout
    layout = ____bindingPattern0.layout
    safeZones = ____bindingPattern0.safeZones
    levelData = ____bindingPattern0.levelData
    self.layout = layout
    self.safeZones = safeZones
    self.levelData = levelData
    self.timeline = __TS__New(Timeline)
end
function Level.prototype.start(self)
    do
        local i = 0
        while i < #self.levelData.safeZones do
            local zoneData = self.levelData.safeZones[i + 1]
            if zoneData.animations then
                for ____, track in ipairs(zoneData.animations) do
                    self:runTrack(track, self.safeZones[i + 1], true)
                end
            end
            i = i + 1
        end
    end
    for ____, shooter in ipairs(self.levelData.shooters) do
        self:setupShooter(shooter)
    end
end
function Level.prototype.destroy(self)
    self.timeline:destroy()
end
function Level.prototype.runTrack(self, track, target, isSafeZone)
    return __TS__AsyncAwaiter(function(____awaiter_resolve)
        local ____temp_0 = self:parseProperty(track.property)
        local group = ____temp_0.group
        local axes = ____temp_0.axes
        local ____track_1 = track
        local keyframes = ____track_1.keyframes
        local loop = ____track_1.loop
        local loopDuration = ____track_1.loopDuration
        if #keyframes == 0 then
            return ____awaiter_resolve(nil)
        end
        local lastKfTime = keyframes[#keyframes].time
        local cycleDuration = loopDuration or lastKfTime
        local function runOnce()
            return __TS__AsyncAwaiter(function(____awaiter_resolve)
                do
                    local i = 0
                    while i < #keyframes do
                        local kf = keyframes[i + 1]
                        local prevTime = i > 0 and keyframes[i].time or 0
                        local duration = kf.time - prevTime
                        if duration == 0 then
                            self:applyValue(
                                target,
                                group,
                                axes,
                                kf.value,
                                isSafeZone
                            )
                        else
                            local tweenTarget = self:buildTweenTarget(axes, kf.value)
                            local tween = self.timeline:tween(target, group, tweenTarget, duration)
                            if isSafeZone then
                                tween:onUpdate(syncSafeZoneTweenCollider)
                            end
                            __TS__Await(self.timeline:wait(duration))
                        end
                        i = i + 1
                    end
                end
            end)
        end
        if loop then
            while true do
                __TS__Await(runOnce(nil))
                local gap = cycleDuration - lastKfTime
                if gap > 0 then
                    __TS__Await(self.timeline:wait(gap))
                end
            end
        else
            __TS__Await(runOnce(nil))
        end
    end)
end
function Level.prototype.parseProperty(self, property)
    if property == "scale" then
        return {group = "scale", axes = {"x", "y"}}
    end
    local dotIndex = (string.find(property, ".", nil, true) or 0) - 1
    return {
        group = __TS__StringSlice(property, 0, dotIndex),
        axes = {__TS__StringSlice(property, dotIndex + 1)}
    }
end
function Level.prototype.buildTweenTarget(self, axes, value)
    local target = {}
    for ____, axis in ipairs(axes) do
        target[axis] = value
    end
    return target
end
function Level.prototype.applyValue(self, obj, group, axes, value, isSafeZone)
    if group == "scale" then
        local s = obj.scale
        local nx = __TS__ArrayIncludes(axes, "x") and value or s.x
        local ny = __TS__ArrayIncludes(axes, "y") and value or s.y
        obj:setScale2D(vmath.vector3(nx, ny, 1))
        if isSafeZone then
            syncSafeZoneCollider(nil, obj)
        end
    elseif group == "euler" then
        go.set(obj.id, "euler.z", value)
    elseif group == "position" then
        local p = obj.position
        local nx = __TS__ArrayIncludes(axes, "x") and value or p.x
        local ny = __TS__ArrayIncludes(axes, "y") and value or p.y
        obj:setPosition2D(vmath.vector3(nx, ny, 0))
    end
end
function Level.prototype.setupShooter(self, data)
    local shooter = self.layout.shooters.factory:create(shooterSchema)
    shooter:setPosition2D(vmath.vector3(data.x, data.y, 0))
    if data.angle ~= 0 then
        go.set(shooter.id, "euler.z", data.angle)
    end
    if data.animations then
        for ____, track in ipairs(data.animations) do
            self:runTrack(track, shooter, false)
        end
    end
    self.timeline:loop(
        data.fireInterval,
        function()
            local count = data.bulletCount or 1
            local spread = data.bulletSpread or 0
            local shooterPos = shooter:getPosition()
            do
                local i = 0
                while i < count do
                    local bullet = self.layout.bullets.factory:create(bulletSchema)
                    bullet:setPosition(shooterPos)
                    local offsetAngle = count > 1 and i * spread - spread * (count - 1) / 2 or 0
                    local rotation = (shooter.angle + offsetAngle) * DEG_TO_RAD
                    local direction = vmath.vector3(
                        math.cos(rotation),
                        math.sin(rotation),
                        0
                    )
                    local distance = 1500
                    local target = shooterPos + direction * distance
                    bullet:animate(
                        Property.Position,
                        target,
                        8,
                        nil,
                        nil,
                        nil,
                        function()
                            bullet:delete()
                        end
                    )
                    bullet.physics:setHandler(
                        PhysicsGroup.wall,
                        function()
                            bullet:delete()
                        end
                    )
                    i = i + 1
                end
            end
        end
    )
end
return ____exports
@modules/core/levels/Level.lualualib_bundlemodules.engine.indexmodules.layouts.BulletLayoutmodules.layouts.ShooterLayout)modules.core.helpers.SyncSafeZoneCollidermodules.enums.PhysicsGroup/lualib_bundle.luac/modules/engine/index.luac"/modules/layouts/BulletLayout.luac#/modules/layouts/ShooterLayout.luac//modules/core/helpers/SyncSafeZoneCollider.luac /modules/enums/PhysicsGroup.luac" 